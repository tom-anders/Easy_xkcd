<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/tvTitleWrapper"
        android:gravity="top|center"
        android:layout_alignParentTop="true" >
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:textColor="@android:color/primary_text_dark"
            android:textSize="@dimen/text_title"
            android:background="@drawable/element_background_box_rounded" />
    </RelativeLayout>

    <ImageView
        android:id="@+id/ivComic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvTitleWrapper"
        android:layout_above="@+id/tvAlt"
        android:layout_gravity="center"
        android:paddingHorizontal="10dp"
        android:paddingVertical="0dp" />

    <TextView
        android:id="@+id/tvAlt"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="bottom|center"
        android:visibility="gone"
        android:padding="10dp"
        android:textSize="@dimen/text_body1"
        android:textColor="@color/fab_material_white"
        android:background="@drawable/element_background_box_rounded" />

</RelativeLayout>